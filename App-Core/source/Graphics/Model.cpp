#include "Graphics/Model.h"

namespace Arc
{
    namespace Graphics
    {
        Model LoadModel(const char* path)
        {
            Model model;

            return model;
        }

        void UnloadModel(Model& model)
        {
        }

    }
}
